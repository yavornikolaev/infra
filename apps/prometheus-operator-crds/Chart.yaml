apiVersion: v2
name: prometheus-operator-crds-wrapper
description: Wrapper chart to install Prometheus Operator CRDs.
type: application
version: 0.1.0

dependencies:
  - name: prometheus-operator-crds
    repository: https://prometheus-community.github.io/helm-charts
    version: ">= 0.0.0"
